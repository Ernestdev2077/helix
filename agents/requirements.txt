# FastAPI
fastapi>=0.115,<1.0
uvicorn[standard]>=0.34,<1.0
pydantic>=2.10,<3.0
pydantic-settings>=2.7,<3.0

# LangGraph + LangChain core
langgraph>=0.2.60,<0.3
langchain-core>=0.3.29,<0.4
langchain>=0.3.14,<0.4
langchain-community>=0.3.14,<0.4
langsmith>=0.2,<1.0

# LLM routing
litellm>=1.57,<2.0

# LLM SDKs (installed but routed through LiteLLM)
openai>=1.59
anthropic>=0.42

# Vector / RAG
pgvector>=0.3,<1.0
asyncpg>=0.30
psycopg[binary]>=3.2

# Redis for pub/sub
redis>=5.2

# HTTP (version set by langsmith/litellm transitive deps)
httpx
tenacity>=9.0

# Structured logging
structlog>=24.4

# Dev
pytest>=8.3
pytest-asyncio>=0.25
ruff>=0.8
mypy>=1.14
